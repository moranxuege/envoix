# @generated from the compiled build manifest (the L4 manifest composed
# with the L5 binding contracts). Do not edit; regenerate with
# `cargo run -p xtask -- record-payload`, which
# scripts/build-jni-libs.sh runs after every cross-compile.
#
# Every identity a release claims to speak. A change here is the
# reviewable record that this build no longer speaks what the last one
# did — the release gate refuses to package a build that drifted from
# this declaration.

[identity]
abi_schema_capability_binding_schema_id = "envoix/binding/capability/1"
abi_schema_command_binding_schema_id = "envoix/binding/command/4"
abi_schema_evidence_rust_abi_id = "envoix/evidence-rust-abi/1"
abi_schema_evidence_timeline_schema_id = "envoix/evidence-timeline/1"
abi_schema_mailbox_receipt_schema_id = "envoix/mailbox/receipt-payload/2"
abi_schema_operation_envelope_schema_id = "envoix/operation-envelope/1"
abi_schema_read_binding_schema_id = "envoix/binding/read/6"
package_version = "0.2.0"
protocol_data_alpn = "656e766f69782f32"
protocol_data_magic = "454e5658"
protocol_data_wire_version = "2"
protocol_set_id = "envoix/protocol-set/2"
trust_root = "unprovisioned"
