# The native payload toolchain identity. scripts/build-jni-libs.sh refuses to
# build with different Rust/Cargo/NDK releases, and this file is itself one of
# release-ledger.toml's payload_sources, so a reviewed bump makes every
# previously recorded payload stale in both Gradle and xtask.
rustc_release=1.96.0
cargo_release=1.96.0
android_ndk_revision=26.3.11579264
