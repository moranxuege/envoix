<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the staged-artifact subtree is ever shared, never the whole
         private root (grants stay artifact-scoped). -->
    <files-path
        name="envoix_artifacts"
        path="state-envoix/artifacts/" />
</paths>
